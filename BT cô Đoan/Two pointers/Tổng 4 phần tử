#include <bits/stdc++.h>
using namespace std;
const int nm = 1e6 + 9;
const int N = 1e3 + 9;
#define el '\n'
#define int long long
int a[nm],res[nm],s; 
signed main (){
	ios_base::sync_with_stdio(false);
	int n,s; cin >> n >> s; 
	for (int i = 1; i <= n ;i++) cin >> a[i]; 
	sort (a + 1, a + 1 + n); 
	for (int i = 1; i < n - 1; i++){
		for (int j = i + 1; j < n; j++){
			int l = j + 1, r = n, cur = s - a[i] - a[j];
			while (l < r){
				int val = a[l] + a[r];
				if (val == cur){
					cout << "YES"; return 0;
				}
				else if (val < cur) l++; 
				else r--; 
			} 
		}
	}
	cout << "NO";
	return 0;
}
