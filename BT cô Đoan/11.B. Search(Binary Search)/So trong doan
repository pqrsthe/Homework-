#include <bits/stdc++.h>
using namespace std;
const int nm = 1e6 + 9;
const int N = 1e3 + 9;
#define el '\n'
int a[nm];

int main (){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);
	int n; cin >> n; 
	for (int i = 1; i <= n; i++) cin >> a[i]; 
	sort (a + 1, a + 1 + n); 
	int q; cin >> q; 
	while (q--){
		int l,r; cin >> l >> r; 
		int le = lower_bound(a + 1, a + 1 + n, l) - a; 
		int ri = upper_bound(a + 1, a + 1 + n, r) - a; 
		--ri; cout << ri - le + 1 << el; 
	}
	return 0;
}
