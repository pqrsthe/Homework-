#include <bits/stdc++.h>
using namespace std;
const int nm = 1e6 + 9;
const int N = 1e3 + 9;
#define el '\n'
int n; 
vector<int> a; 

void Print(vector <int> v){
	cout << n << "="; 
	for (int i = 0; i < v.size() - 1; i++)
		cout << v[i] << "+"; 
	cout << v[v.size() - 1]; cout << el; 
}

void bt(int sum, int i, vector<int> v){
    if (sum > n){
    	return; 
	}
	if (sum == n){
		Print(v); 
		
	}
	for (int j = i; j <= n; j++){
		v.push_back(j); 
		bt(sum + j, j, v); 
		v.pop_back(); 
	}
}

signed main (){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);
	cin >>n ; 
	bt(0,1,a); 
	return 0;
}
