#include <bits/stdc++.h>
using namespace std;
const int nm = 1e6 + 9;
const int N = 1e3 + 9;
#define el '\n'
map<string,int> mp;
string res; 

int main (){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);
	mp["Bessie"] = 0; mp["Elsie"] = 0; mp["Daisy"] = 0; 
	mp["Gertie"] = 0; mp["Annabelle"] = 0; mp["Maggie"]= 0; mp["Henrietta"]=0;
	int n; cin >> n; 
	for (int i = 1; i <= n; i++){
		string a; int b; cin >> a >> b; 
		mp[a] += b; 
	}
	int mn = 1e9;
	for (auto it : mp){
		mn = min(mn , it.second); 
	}
	int ans = 1e9; 
	for (auto it : mp){
		if (it.second > mn){
			ans = min(ans , it.second);
		}
	}
	int dem = 0; 
	for (auto it : mp){
		if (ans == it.second){
			dem++; res = it.first; 
		}
	}
	if (dem > 1) cout << "Tie"; 
	else cout << res; 
	return 0;
}
