#include <bits/stdc++.h>

using namespace std;

#define ll long long
#define el '\n'
#define st first
#define nd second

const int nm = 1e5 + 9;
const long long INF = 1e18;
const int MOD = 1e9 + 7;
const int N = 2e3 + 9;
priority_queue<int,vector<int>,greater<int>> pq; 
map <int,int> st;
int a[nm];  
void solve(){
	int n; cin >> n;
	for (int i = 1; i <= n; i++){
		cin >> a[i]; 
		if (st[a[i]] == 0){
			st[a[i]] = i; 
		}
		pq.push(st[a[i]]); 
	}
	while (!pq.empty()){
		int mx = pq.top(); 
		if (mx == pq.top()){
			while (!pq.empty() && mx == pq.top()) pq.pop(); 
			cout << a[mx] << " "; 
		}
	
	}
}

signed main (){
	int t; t = 1;
	while (t--){
		solve();
	}
	return 0;
}
