#include <bits/stdc++.h>

using namespace std;

#define ll long long
#define el '\n'
#define st first
#define nd second

const int nm = 1e5 + 9;
const long long INF = 1e18;
const int MOD = 1e9 + 7;
const int N = 2e3 + 9;
priority_queue<int,vector<int>,greater<int>> pq; 
map <int,int> st;
int a[nm];  
map<string,string> mp; 
void solve(){
	int n; cin >> n;
	for (int i = 1; i <= n; i++){
		string fi,se; cin >> fi >> se; 
		mp[se] = fi; 
	}
	int m; cin >> m; 
	for (int i = 1; i <= m; i++){
		string a,b; cin >> b; 
		if (mp[b] != "") cout << mp[b] << el; 
		else cout << "eh" << el; 
	}
}

signed main (){
	int t; t = 1;
	while (t--){
		solve();
	}
	return 0;
}
